namespace LSTY.Sdtd.ServerAdmin.RpcClient.Clients
{
    public enum ConnectionState
    {
        Disconnected,
        Connecting,
        Connected,
        Disconnecting,
        Disposing,
        Disposed
    }
}
